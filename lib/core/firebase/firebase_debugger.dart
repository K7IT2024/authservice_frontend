class FirebaseDebugger {
  /// Firebase removed. This project now uses AWS SNS for OTP delivery.
  /// Keep this file as a placeholder. For SMS debug, check backend logs.

  static void printMessage() {
    print('Firebase has been removed. Use AWS SNS via backend for OTP.');
  }
}

// Example usage in your debug console:
// await FirebaseDebugger.printFirebaseConfig();
// await FirebaseDebugger.testPhoneOtp("+919876543210");
// FirebaseDebugger.printSolutions();

